export interface IDepartmentBase {
  // immutable data which has to kept safe
  deptId: number;
  deptName:string;
}

export interface IDepartment {
  // immutable data which has to kept safe
  deptId: number;
  deptName: string;
}
